# NMSPL Branch Locator

Static site (HTML/CSS/JS) ready for Netlify.
- Open `index.html` as the site entry.
- `about.html` is an info page.
- `style.css` and `script.js` hold styles and logic.
- Uses Leaflet CDN for the map; no server required.
